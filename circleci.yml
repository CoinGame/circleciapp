# Configuration to run at least some lint on CircleCi
# https://circleci.com/docs/1.0/configuration/

machine:
  php:
    version: 5.6.22

test:
  override:
    # Very crude, could sanitize output. Not worth it in bash
    find . -type f -name "*.php" -print0 | xargs -0 -n1 php -l
